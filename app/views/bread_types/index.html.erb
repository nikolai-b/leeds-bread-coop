<h1>Listing Bread Types</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Sour dough</th>
      <th>Wholesale Only</th>
      <th>Notes</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @bread_types.each do |bread_type| %>
      <tr>
        <td><%= link_to bread_type.name, bread_type %></td>
        <td><%= bread_type.sour_dough %></td>
        <td><%= bread_type.wholesale_only %></td>
        <td><%= bread_type.notes %></td>
        <td><%= link_to 'Edit', edit_bread_type_path(bread_type), class: 'btn btn-mini'  %></td>
        <td><%= link_to 'Destroy', bread_type, method: :delete, class: 'btn btn-mini btn-danger', data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bread type', new_bread_type_path, class: 'btn btn-mini btn-success' %>
