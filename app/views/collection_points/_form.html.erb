<%= form_for(@collection_point) do |f| %>
  <% if @collection_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collection_point.errors.count, "error") %> prohibited this collection_point from being saved:</h2>

      <ul>
      <% @collection_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :post_code %><br>
    <%= f.text_area :post_code %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
