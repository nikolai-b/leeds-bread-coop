<h1>Delivary Report</h1>

<table>
  <thead>
    <tr>
      <th>Collection Point</th>
      <th>Subscriber</th>
      <th>Bread Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @delivery_report.try(:each) do |delivery| %>
      <% next unless delivery.any? %>
      <tr>
        <td><strong><%= delivery[0].collection_point.name %></strong></td>
      </tr>
      <% delivery.try(:each) do |subscriber| %>
        <tr>
          <td> </td>
          <td><%= link_to(subscriber.name, subscriber) %></td>
          <td><%= subscriber.bread_type.name %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>
